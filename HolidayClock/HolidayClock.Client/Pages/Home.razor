@inject NavigationManager _navigationManager
@rendermode @(new InteractiveAutoRenderMode(prerender: false)) 
@page "/"

@code {
    protected override void OnInitialized()
        {
    _navigationManager.NavigateTo("/newyears");
    }
}